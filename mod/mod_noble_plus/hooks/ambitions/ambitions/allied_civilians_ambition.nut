// =============================================================================
// Хук подавления ванильной амбиции "allied_civilians_ambition"
// =============================================================================
// Ванильная амбиция: "Союз с поселением"
// Подавляем для сценария "Дворянин +": в нашей нарративной линии Главы I
// игрок занят сбором отряда, золота и известности, и ванильный квест
// союза с поселением не вписывается в нарратив.

::mods_hookExactClass("ambitions/ambitions/allied_civilians_ambition", function(o)
{
    local origUpdate = o.onUpdateScore;
    o.onUpdateScore = function()
    {
        // Для сценария "Дворянин +" — не показывать эту ванильную амбицию
        if (this.World.Assets.getOrigin().getID() == "scenario.noble_plus") return;
        // Для всех остальных сценариев — ванильное поведение
        origUpdate();
    };
});
