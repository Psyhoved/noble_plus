# Noble Plus — Дорожная карта

Статусы: ✅ DONE | 🔄 IN PROGRESS | ⏳ TODO | 🔮 FUTURE

---

## Фаза 0 — Инфраструктура ✅ DONE

MCP Filesystem подключён к четырём директориям. ✅
BB MCP Server написан, подключён, работает (launch_game, kill_game, read_game_log). ✅
Путь к exe игры: `win32\BattleBrothers.exe` (не в корне, а в подпапке). ✅

---

## Фаза 1 — Исследование кодовой базы ✅ DONE

Изучена система компаньонов-животных в Legends. ✅
Изучена механика game over (Lone Wolf). ✅
Изучена структура сценариев и как они регистрируются движком. ✅
Все паттерны задокументированы в TECH.md. ✅

---

## Фаза 2 — Архитектура мода ✅ DONE

Создана правильная структура файлов в `data/scripts/`. ✅
Мод регистрируется и загружается без ошибок. ✅
Класс сценария создаётся (`create()` вызывается). ✅

---

## Фаза 3 — Разработка по этапам

### Этап 3.1 — Скелет сценария 🔄 IN PROGRESS (СЛЕДУЮЩИЙ ШАГ)

**Что уже работает:** мод загружается, класс `noble_plus_scenario` создаётся.
Исправление применено: убран вызов `this.starting_scenario.create()`.
Добавлены методы `onInit()`, `onSpawnAssets()`, `onSpawnPlayer()`, `onCombatFinished()`.

**Что нужно проверить:**

Запустить игру, открыть меню выбора происхождений — убедиться что "Дворянин +" виден с правильным текстом и картинкой.
Запустить кампанию — убедиться что отряд появляется на карте без зависания.
Проверить лог на отсутствие ошибок уровня ERROR с тегом SQ.

Подробный план исправления задокументирован в `TODO_current.md`.

Цель этапа считается выполненной когда: мод грузится без ошибок И сценарий виден в меню И кампания стартует без зависания.

---

### Этап 3.2 — Стартовый состав отряда ⏳ TODO

Прописать дворянина: аватар с титулом "Сэр", черта Player, флаг IsPlayerCharacter, белый волк (`legend_white_wolf_item`) в accessory slot.
Прописать слугу: фоновое происхождение `servant_background`, кинжал как дополнительное оружие.
Обновить описание сценария (`m.Description`) на текст из NARRATIVE.md (предлагаемое новое описание).
Уточнить: нужен ли третий стартовый персонаж (смотреть ванильный Disowned Noble).
Цель: запустить новую игру и увидеть правильный стартовый отряд.

---

### Этап 3.3 — Стартовое событие (Пролог) ⏳ TODO

**Нарратив:** Текст и структура пролога описаны в NARRATIVE.md, раздел "Часть I: Пролог — Глава 0".

Создать файл события `noble_plus_intro_event.nut` в `scripts/!mods_preload/events/` или аналогичном пути.
Событие срабатывает один раз при старте кампании — через `scheduleEvent` из `onSpawnPlayer()`.
Текст события: "Три месяца назад ты был наследником Дома Локвуд..." (полный текст в NARRATIVE.md).
Картинка: `gfx/ui/events/event_176.png`.

**Технический паттерн:** Вызов в `onSpawnPlayer()`:
```squirrel
this.Time.scheduleEvent(this.TimeUnit.Real, 1000, function(_tag) {
    this.World.Events.fire("event.noble_plus_intro");
}, null);
```

Цель: при старте кампании игрок видит вступительный текст про барона Локвуда.

**Связанная FUTURE задача:** Динамическая подача текста — см. Фазу 4.

---

### Этап 3.4 — Система амбиций Главы I ⏳ TODO

**Это переход к полноценному моддингу.** Амбиции — главный механизм, которым мы двигаем игрока по сюжету в открытом мире BB. Нарратив и последовательность амбиций описаны в NARRATIVE.md, раздел "Часть II: Глава I".

**Четыре последовательные амбиции:**

| # | Амбиция | Условие завершения | Нарративный смысл |
|---|---------|-------------------|-------------------|
| 1.1 | "Нанять людей" | 8+ бойцов в ростере | Барон собирает армию |
| 1.2 | "Заработать золото" | 2000+ крон | Финансирование похода |
| 1.3 | "Найти союзников" | Союз с одним знатным домом | Политическая подготовка |
| 1.4 | "Вернуть родовой замок" | Победа над узурпатором | Финал Главы I |

**Архитектура реализации:**

Создать отдельный файл `noble_plus_ambitions.nut` — модульный файл системы амбиций.
Зарегистрировать его через MSU: панель настроек в меню мода (включить/выключить отдельные амбиции).
Амбиции активируются последовательно: каждая следующая открывается после завершения предыдущей.
Состояние прогресса сохраняется в World.Flags: `NoblePlus.Ambition.CurrentChapter`, `NoblePlus.Ambition.CurrentStage`.

Изучить паттерн амбиций в Legends (файлы `scripts/ambitions/`) и в ванили.
Изучить MSU Settings API для добавления настроек мода — образец: `mod_clever_recruiter`.

Цель: в игре последовательно появляются 4 сюжетных амбиции, каждая с нарративным текстом.

---

### Этап 3.5 — Механика конца игры ⏳ TODO

Добавить `onCombatFinished` по образцу Lone Wolf (паттерн задокументирован в TECH.md).
Проверить что смерть дворянина завершает игру.
Уточнить: нужна ли обработка смерти вне боя.

---

### Этап 3.6 — Шлифовка ⏳ TODO

Финальная проверка описания, баланса стартового снаряжения.
Проверка совместимости с другими установленными модами.
Тест полного сценария от начала до первого боя.

---

## Фаза 4 — Будущие возможности 🔮 FUTURE

**Динамическая подача текста (визуальная новелла).**
Сейчас тексты событий в BB — стена текста. Цель: сделать вступительный пролог и ключевые события интерактивнее — текст разбивается на реплики и подаётся по ролям или кускам, с паузами между. Возможные подходы:
- Последовательная цепочка событий (каждое событие = одна реплика), переход по кнопке
- Хук на UI с кастомным рендерингом текста через кадрированную анимацию
Это сложная задача (требует глубокого изучения UI-движка BB). Реализуется после завершения базового пролога (Этап 3.3).

**Захват замка (на основе Stronghold 2).**
Дворянин возвращается в родной замок и захватывает его. Зацепка в коде: событие `disowned_noble_welcomed_back_event.nut` и дерево перков Noble Usurper в Legends.

**Глава II — "Война Домов".**
После взятия замка: признание со стороны соседних баронов, политические союзы, возможный брак. Нарративная канва описана в NARRATIVE.md (Часть III). Требует реализации Главы I.

**Механики владения землёй (CK-стиль).**
Династии, браки, передача земель, вассалитет. Каждая фича — отдельный модуль. Дальнее будущее.

---

## Технический долг и открытые вопросы

MSU Settings API для версии 1.7.2 — нужен для Этапа 3.4. Изучить другие установленные моды (например `mod_clever_recruiter`) — они используют MSU Settings и могут служить образцом.

Паттерн амбиций в Legends — нужно найти и изучить перед реализацией Этапа 3.4.

Состав ванильного стартового отряда Disowned Noble — недоступен без распаковки `.dat` файлов. Можно посмотреть в игре или найти на вики прежде чем писать `onSpawnAssets`.

Поведение механики game over при смерти вне боя — нужен ли отдельный хук помимо `onCombatFinished`?
