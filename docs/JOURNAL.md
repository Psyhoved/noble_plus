# Noble Plus — Журнал разработки

## Как читать и вести этот журнал

Этот файл устроен в два слоя. Верхний слой — **краткая история проекта**: сжатое резюме всего, что было сделано до последних нескольких сессий. Нижний слой — **детальные записи последних 3-5 сессий** в полном виде.

Когда детальных записей накапливается больше пяти, агент в конце сессии обязан провести сжатие: самую старую детальную запись превратить в одну-две фразы в разделе "Краткая история", а её полный текст перенести в `JOURNAL_ARCHIVE.md`. Так журнал никогда не разрастается до размеров, засоряющих контекст.

Важное правило: в журнал попадают только **решения и причины**, а не просто список действий. "Создали файл X" — это не журнальная запись. "Решили использовать белого волка вместо вархаунда, потому что он сильнее и нарративно точнее соответствует образу родового зверя благородного дома" — это журнальная запись. Следуя этому правилу, журнал остаётся компактным и полезным даже спустя месяцы.

---

## Краткая история проекта

**Сессия 1 (20 февраля 2026):** Сформулирована концепция мода, настроена вся инфраструктура (MCP Filesystem + BB MCP Server), проведено исследование файлов Legends. Выбран легендарный белый волк как компаньон дворянина. Все ключевые паттерны кода задокументированы в TECH.md.

**Сессия 3 (20 февраля 2026, вечер):** Найдена и задокументирована корневая причина краша при старте кампании: вызов `this.starting_scenario.create()` в методе `create()` — ни один рабочий сценарий Легенд его не вызывает. Проанализированы 4 рабочих сценария из архива Легенд. Задокументированы 6 нерушимых правил в TECH.md. Исправление не применено — лимит токенов.

---

## Детальные записи сессий

### Сессия 5 — 21 февраля 2026

**Что сделано.**

Реализован полный стартовый состав отряда (Этапы 3.1–3.3 закрыты).

Добавлены 6 персонажей — точная копия `legends_noble_scenario` из Легенд 19.1.47: командир-дворянин (`legend_noble_commander_background`), два щитоносца (`legend_noble_shield`), двуручник (`legend_noble_2h`), слуга (`servant_background`), стрелок (`legend_noble_ranged`). Добавлены `onHiredByScenario`, `onUpdateHiringRoster`, `onGenerateBro`, `onBuildPerkTree` для корректного поведения пула найма.

Дворянину выдан **Лютоволк** (`legend_white_wolf_item`). Имя фиксировано: `wolf.m.Name = "Лютоволк"` — по умолчанию элемент генерировал случайное имя из `Const.Strings.WardogNames + ", белый волк"`.

Слуге выдан **кинжал** (`dagger.nut`) в основную руку.

В `onSpawnPlayer` добавлены: отрицательные отношения с ближайшим благородным домом (−100), баннер дома на щиты братьев[1] и [3], табарды `legend_noble_tabard` всем шести.

**Ключевые решения.**

Использован `legends_noble_scenario` как эталон для состава, а не ванильный `disowned_noble_scenario` — первый полностью доступен в архиве Легенд, второй спрятан в бинарных `.dat` файлах. К тому же наш мод строится поверх Легенд, поэтому их noble-backgrounds являются правильными зависимостями.

`m.Name` предмета доступен напрямую после `this.new()` — поля `m` изменяемы без хуков.

**Что переходит в следующую сессию.**

Этап 3.4 — шлифовка: финальный тест полного сценария (включая бои), проверка совместимости с другими модами, возможная замена картинки сценария.

---

### Сессия 4 — 21 февраля 2026

**Что сделано.**

Восстановлен контекст вчерашней работы через логи сессии. Применено исправление: удалён вызов `this.starting_scenario.create()` из `noble_plus_scenario.nut` — именно этот вызов был корневой причиной краша. Обновлены TECH.md (исправлен неправильный пример, добавлена таблица 6 правил и полный шаблон) и JOURNAL.md.

**Статус на начало тестирования:**

Файл `noble_plus_scenario.nut` исправлен. Структурно код соответствует паттерну рабочих сценариев Легенд. Готов к первому запуску.

**Что переходит в следующую сессию.**

Запустить игру, выбрать сценарий "Дворянин +", убедиться что кампания создаётся без краша. Затем — добавить белого волка в `onSpawnAssets()` и проверить. Затем — задокументировать сессию и двигаться к следующей фазе роадмапа.

---

### Сессия 2 — 20 февраля 2026 (продолжение)

**Что обсуждали и решили.**

Принято решение вести документацию как персистентную память с механизмом сжатия журнала: детальные записи хранятся для последних 3-5 сессий, более старые сжимаются до одной-двух фраз в разделе "Краткая история" и архивируются в `JOURNAL_ARCHIVE.md`. Правило наполнения журнала: только решения и причины, не список действий.

Настроен проект в Claude Desktop — все будущие чаты по разработке создаются внутри него, что обеспечивает общий контекст и позволит в будущем настроить системный промпт проекта.

**Критические технические открытия этой сессии.**

Первое открытие — **структура виртуальной файловой системы BB**. Движок использует `data/` как корень виртуальной FS. ZIP-архивы модов виртуально "распаковываются" прямо в этот корень. Поэтому preload-скрипты должны лежать в `data/scripts/!mods_preload/`, а НЕ в `data/mod_noble_plus/scripts/!mods_preload/` — в последний путь движок не смотрит.

Второе открытие — **как движок регистрирует сценарии**. Движок сканирует `scripts/scenarios/world/` при раннем старте и создаёт экземпляры всех найденных классов — именно тогда вызывается `create()`. Это происходит ДО запуска очереди `mods_queue`. Если дополнительно вызвать `::include(...)` на тот же файл внутри `mods_queue`, файл загружается второй раз и переопределяет класс, но движок уже собрал список сценариев — второй вариант класса игнорируется. Вывод: файл сценария НЕ должен включаться через `::include` в preload, движок подхватывает его сам.

Третье открытие — **правильный родительский класс для кастомного сценария**. Мы сначала унаследовали от `disowned_noble_scenario` (DLC8), но это вызвало проблемы совместимости. Правильный базовый класс для кастомных сценариев Легенд — `starting_scenario`. Именно от него наследуют все кастомные сценарии в Legends (`legends_free_company_scenario`, `legends_mage_scenario` и др.).

Четвёртое открытие (незакрытое) — **ошибка MSU Settings API**. При попытке добавить панель настроек через `::NoblePlus.Mod.Settings` получили ошибку: `the index 'Settings' does not exist`. Это означает что либо API называется иначе, либо нужно сначала инициализировать объект настроек другим способом. Требует изучения документации MSU в следующей сессии.

**Статус на конец сессии.**

Мод регистрируется и загружается (`Noble Plus 0.1.0 загружен.` в логе). Класс сценария создаётся (`noble_plus_scenario::create() вызван.` в логе). Но игра падает при старте из-за ошибки в коде настроек MSU. Сценарий в меню выбора происхождений пока не проверен — нужно сначала убрать падающий код настроек.

**Что переходит в следующую сессию.**

Первый шаг: убрать или исправить блок `Settings` в preload-файле — он вызывает краш. Второй шаг: убедиться что сценарий "Дворянин +" появляется в меню выбора происхождений. Третий шаг: разобраться с правильным API MSU Settings и добавить панель настроек правильно.
